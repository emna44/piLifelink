pipeline {
    agent any

    environment {
        // Replace these with your actual server details
        SERVER_USER = 'ubuntu'  // Your actual server username
        SERVER_IP = '192.168.1.100'  // Your actual server IP
        SERVER_DIR = '/home/ubuntu/piLifelink'  // Actual deployment directory
    }

    stages {
        stage('Clone Repo') {
            steps {
                echo 'Cloning source code...'
                checkout scm
            }
        }

        stage('Build React App') {
            steps {
                dir('template') {
                    sh 'npm install'
                    sh 'CI=false npm run build'
                }
            }
        }

        stage('Build Node App') {
            steps {
                dir('server') {
                    sh 'npm install'
                }
            }
        }

        stage('Copy Files to Server') {
            steps {
                sshagent(['57c7b439-bddc-42d2-a693-0f93fcd6f1d']) {
                    // Add your scp or rsync commands here, e.g.:
                    // sh 'scp -r build/* ${SERVER_USER}@${SERVER_IP}:${SERVER_DIR}/frontend'
                    // sh 'scp -r ../server/* ${SERVER_USER}@${SERVER_IP}:${SERVER_DIR}/server'
                    echo 'Files copied to server (add scp commands here)'
                }
            }
        }

        stage('Deploy on Server') {
            steps {
                sshagent (credentials: ['your-ssh-credential-id']) {
                    sh """
                        ssh ${SERVER_USER}@${SERVER_IP} '
                        cd ${SERVER_DIR} &&
                        docker-compose down &&
                        docker-compose up -d --build
                        '
                    """
                }
            }
        }
    }
}
